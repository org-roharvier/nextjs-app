name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened, edited, assigned, unassigned, labeled, unlabeled]

jobs:
  ci-checks:
    uses: org-roharvier/cicd-workflows-claude/.github/workflows/ci.yml@main
    with:
      enable-label-check: true
